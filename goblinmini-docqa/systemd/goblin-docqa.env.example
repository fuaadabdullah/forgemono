### Example systemd environment file for Goblin Mini DocQA
# Copy this to /etc/default/goblin-docqa and edit values as required
# Ensure the file is readable by the service user (defaults: root-owned)

# API token used by the server for requests
DOCQA_TOKEN=supersecret

# Root directory for allowed file access (mounted or local)
DOCQA_ROOT=/mnt/allowed

# Port is irrelevant if using UDS. Used for health checks and metrics where applicable
# Keep it in case you prefer TCP-only operation during development
DOCQA_PORT=9000

# Local model enabling
DOCQA_ENABLE_LOCAL_MODEL=false

### Optional tuning
# Maximum queue size and worker tuning are controlled via environment variables
MAX_INFERENCE_WORKERS=1
MAX_QUEUE_SIZE=8
