# Local LLM Deployment

This folder contains scripts for deploying the LLM locally.

- **local_deploy.sh**: Deploys the LLM to a local environment with Kamatera server access.
  - Usage: `./local_deploy.sh`
