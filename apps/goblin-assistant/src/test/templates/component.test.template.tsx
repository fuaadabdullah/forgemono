import React from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import { describe, it, expect } from '@jest/globals';

// Template for testing simple presentational components
// - Use render() from testing-library/react
// - Use screen.getBy* to query DOM
// - Use fireEvent to simulate user actions

describe('MyComponent (template)', () => {
  it('renders with required props and responds to clicks', () => {
    // const onClick = jest.fn();
    // render(<MyComponent prop1={true} onClick={onClick} />);
    // fireEvent.click(screen.getByRole('button'));
    // expect(onClick).toHaveBeenCalled();
  });
});
