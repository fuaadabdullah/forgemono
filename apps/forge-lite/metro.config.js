// Minimal ESM metro config stub. We keep the authoritative CJS config in
// `metro.config.cjs`. Exporting an empty default object here avoids a
// Node load error on environments that try to load this file.

export default {};
