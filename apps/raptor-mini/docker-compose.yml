version: '3.8'
services:
  raptor:
    build: .
    image: raptor-mini:dev
    ports:
      - '8080:8080'
    environment:
      - API_KEY=${RAPTOR_API_KEY}
      - MODEL_NAME=raptor-mini
    volumes:
      - ./models/raptor-mini:/models/raptor-mini:ro
    restart: unless-stopped
