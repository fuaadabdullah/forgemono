# D1 Database Configuration (SQLite at the edge)
# Use for: structured data, user preferences, audit logs
# Run: wrangler d1 create goblin-assistant-db

# To create the database:
# wrangler d1 create goblin-assistant-db

# To run migrations:
# wrangler d1 execute goblin-assistant-db --file=./schema.sql

# Example binding (uncomment after creating D1 database):
# [[d1_databases]]
# binding = "DB"
# database_name = "goblin-assistant-db"
# database_id = "your-database-id"

# Example schema.sql:
# CREATE TABLE user_preferences (
#   user_id TEXT PRIMARY KEY,
#   theme TEXT DEFAULT 'dark',
#   language TEXT DEFAULT 'en',
#   model_preference TEXT,
#   created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#   updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
# );
#
# CREATE TABLE audit_logs (
#   id INTEGER PRIMARY KEY AUTOINCREMENT,
#   user_id TEXT,
#   action TEXT,
#   details TEXT,
#   ip_address TEXT,
#   timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
# );
#
# CREATE INDEX idx_user_id ON audit_logs(user_id);
# CREATE INDEX idx_timestamp ON audit_logs(timestamp);
