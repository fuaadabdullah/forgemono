tunnel: 9c780bd1-ac63-4d6c-afb1-787a2867e5dd
credentials-file: /Users/fuaadabdullah/ForgeMonorepo/apps/goblin-assistant/infra/cloudflare/goblin-tunnel-creds.json

ingress:
  # Admin Dashboard (Zero Trust protected)
  - hostname: admin.yourdomain.com
    service: http://localhost:8000
    originRequest:
      access:
        required: true
        teamName: your-team-name
        audTag: [ "your-access-app-aud-tag" ]

  # Backend API
  - hostname: api.yourdomain.com
    service: http://localhost:8000

  # Catch-all
  - service: http_status:404
